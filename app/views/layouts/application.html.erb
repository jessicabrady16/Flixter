<!DOCTYPE html>
<html lang = 'en'>
  <head>
    <title>Flixter</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div id="flex-content-container">
      <nav class="navbar navbar-toggleable-md navbar-light bg-faded">
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <%= link_to 'Flixter', root_path, class: 'navbar-brand col-2' %>
        <div class="navbar-collapse" id="navbarTogglerDemo02">
          <ul class="navbar-nav mr-auto mt-2 mt-md-0">
            <li class="nav-item">
              <%= link_to 'See all Courses', courses_path, class: 'nav-link' %>
            </li>
          </ul>
          <ul class="navbar-nav">
            <li class="nav-item">
              <% if user_signed_in? %>
                <li class="nav-item btn btn-sm btn-outline-secondary">
                  <%= link_to 'Sign Out', destroy_user_session_path, class: 'nav-link' %>
                </li>
              <% else %>
                <li class="nav-item btn btn-sm btn-outline-secondary">
                  <%= link_to 'Sign In', new_user_session_path, class: 'nav-link' %>
                </li>
                <li class="nav-item btn btn-sm btn-outline-secondary">
                  <%= link_to 'Sign Up', new_user_registration_path, class: 'nav-link' %>
                </li>
              <% end %>
            </ul>
          </div>
        </nav>
        <% if notice.present? %>
          <p class="alert alert-info"><%= notice %></p>
        <% end %>
        <% if alert.present? %>
          <p class="alert alert-danger"><%= alert %></p>
        <% end %>
        <%= yield %>
      </div>
      <div id="footer">
        <div class="container">
          <b>Teachers</b>
          <br />
          <%= link_to 'Teach a Course', new_instructor_course_path, class: "underline" %>
        </div>
      </div>
    </body>
  </html>
